#include <stdio.h>

int main() {
	int a = 2; // 초기화
	int b = 3;

	printf("%d + %d =  %d\n", a, b, a + b);

	// 변수 이름 : 알파멧 대소문자, _(underscore), 숫자(첫번째 글자로는 사용불가)
	int abc;
	//int 3a; 불가
	int a2;
	int AAAA;
	int unser_score;
}